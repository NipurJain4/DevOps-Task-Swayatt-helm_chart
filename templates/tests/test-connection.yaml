apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "swayatt-chart.fullname" . }}-test-connection"
  labels:
    {{- include "swayatt-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "swayatt-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
