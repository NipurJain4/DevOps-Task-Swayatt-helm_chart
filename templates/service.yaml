apiVersion: v1
kind: Service
metadata:
  name: {{ include "swayatt-chart.fullname" . }}
  labels:
    {{- include "swayatt-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "swayatt-chart.selectorLabels" . | nindent 4 }}
